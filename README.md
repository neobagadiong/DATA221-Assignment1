DATA 221 - Assigment 1 
Author: Neo Bagadiong 
UCID: 30141578

Collection of py files for each submission for assignment 1.

Question 1:\
A loop that will multiply the current number set with the subsequent integer until the current number surpasses the threshold set.

Question 2:\
A function that takes a list of strings and creates a dictionary with the string as the key and the value being a dictionary containing the strings's length and parity.

Question 3:\
A function that takes a list of lists containing 2 integers, x and y. The function computes x^y for entries with non-negative y.

Question 4:\
Creates a list of random values between 0 and 1, then sorts the list and determines the indeces of values larger than a randomly assigned x.

Question 5:\
A function that compares 2 circles, entered as its radius. Smaller circle is always compared to the larger.

Question 6:\
A funtion that takes an input of integers and returns a dictionary containing the percentage of elements in list that the integer is equal or greater to.

Question 7:\
A function that takes seconds elapsed since midnight and converts it to Hrs:Min:Sec AM/PM (12hr format).

Question 8:\
Takes provided data and creates pandas DataFrame and creates a new column using other column data.        
